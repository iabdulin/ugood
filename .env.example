# Database Credentials (used by db and backend services)
POSTGRES_DB=ugood_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres # Use a secure password

# Rails Master Key (get from config/master.key or generate if needed)
# RAILS_MASTER_KEY=your_rails_master_key_content_here

# Add other secrets or environment-specific settings here
